# ⚡ labels every PR by touched paths (.github/labeler.yml). sync-labels
# keeps them honest when a PR's scope shrinks.
name: pr-labeler
on:
  - pull_request_target

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v7
        with:
          sync-labels: true
