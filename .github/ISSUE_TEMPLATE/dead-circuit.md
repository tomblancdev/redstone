---
name: "🧨 Dead circuit"
about: Something that should power doesn't (or shouldn't, and does)
labels: bug
---

## What doesn't power

<!-- The bind/command/behavior that fails. Paste the actual refusal/output —
     refusals carry their reasons on purpose. -->

## What you expected

## How to reproduce

```sh
# commands, minimal registry snippet (stacks/*.yaml) if relevant
```

## World info

- `redstone version` output:
- OS / how it runs (binary, container):
- Registry: file-form / folder-form stacks, roughly how many:

<!-- Security issue? Don't file it here — see SECURITY.md. -->
